@model UI.Models.CreateLineModel

@{
    ViewData["Ids"] = "Tilføj produkt";
}

<h1>Tilføj produkt</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddPluklistLine">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            @*<div class="form-group">
                <label asp-for="ProductId" class="control-label"></label>
                <input asp-for="ProductId" class="form-control" />
                <span asp-validation-for="ProductId" class="text-danger"></span>
            </div>*@
             <div class="form-group">
                <label asp-for="Ids" class="control-label"></label>
                <select class="form-select" asp-items="Ids" asp-for="Ids" name="">
                    @foreach (var item in Model.Ids)
                    {
                        <option value="@item.Ids">@item.Ids</option>
                    }
             </select>
                 <br/>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}